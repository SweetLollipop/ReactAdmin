import React, { Component } from 'react';

export default class Product extends Component {
    state = {  }
    render() {
        return (
            <div>
                商品分类组件
            </div>
        );
    }
}